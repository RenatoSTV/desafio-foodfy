<div class="item">
    <label>Nome</label>
    <div class="input">
        <input 
            type="text" 
            name="name" 
            placeholder="Digite o nome de usuário"
            value="{{user.name}}"
            required
        >
    </div>
</div>

<div class="item">
    <label>E-mail</label>
    <div class="input">
        <input 
            type="email" 
            name="email" 
            placeholder="Digite o email"
            value="{{user.email}}"
            onblur="Validate.apply(this, 'isEmail')"
            required
        >
    </div>
</div>

